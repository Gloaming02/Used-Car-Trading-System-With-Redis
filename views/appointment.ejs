<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/flatpickr@4.6.3/dist/flatpickr.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            margin-bottom: 60px; 
        }

        .container {
            margin-top: 20px;
        }

        .schedule-container {
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 5px;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            margin-bottom: 10px;
        }

        footer {
            background-color: #343a40;
            color: #fff;
            padding: 10px;
            text-align: center;
            position: fixed;
            bottom: 0;
            width: 100%;
        }
    </style>
</head>

<body>

    <div class="container schedule-container">
        <h2>Appointment Schedule</h2>
        <p>Car ID: <%= carId %></p>
        <p>Seller ID: <%= sellerId %></p>
        <p>Please choose a date and time for your appointment:</p>
        <form id="appointmentForm" action="/appointment" method="post">
            <input type="text" id="datetimepicker" name="appointment_time" placeholder="Select date and time" required>
            <button type="submit" class="btn btn-primary mt-3" onclick="submitAppointment(event)">Submit Appointment</button>
        </form>
    </div>

    <footer>
        <div class="container">Made by Zhengyan Hu with 💪🏼</div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
    <script>
        let tomorrow = new Date();
        let nextWeek = new Date();
        nextWeek.setDate(nextWeek.getDate() + 7);
        tomorrow.setDate(tomorrow.getDate() + 1);
        flatpickr("#datetimepicker", {
            enableTime: true,
            dateFormat: "Y-m-d",
            minDate: tomorrow,
            maxDate: nextWeek,
        });

        function submitAppointment(event) {
            event.preventDefault(); // 阻止默认的表单提交行为
            const form = document.getElementById('appointmentForm');
            const formData = new FormData(form);

            const carId = "<%= carId %>";
            const sellerId = "<%= sellerId %>";
            const appointmentTime = formData.get('appointment_time');

            const url = `/appointment?carId=${carId}&sellerId=${sellerId}&appointmentTime=${appointmentTime}`;
            window.location.href = url;
        }
    </script>
    
    
    
</body>

</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Include necessary head content -->
</head>
<body>
    <div class="container">
        <h2 style="text-align: center;">Make Appointment</h2>
        <% if(errorMessage) { %>
            <div class="alert alert-danger" role="alert">
                <%= errorMessage %>
            </div>
        <% } %>
        <form method="post" action="/appointment">
            <input type="hidden" name="car_id" value="<%= carId %>">
            <input type="hidden" name="seller_id" value="<%= sellerId %>">
            <div class="form-group">
                <label for="date" class="form-label">Date</label>
                <input type="date" class="form-control" id="date" name="date" required>
            </div>
            <input type="hidden" name="customer_id" value="<%= customerId %>">
            <button type="submit" class="btn btn-primary btn-appointment">Submit</button>
        </form>
    </div>
    <!-- Include necessary scripts -->
</body>
</html>
