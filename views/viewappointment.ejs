<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            margin-bottom: 60px;
        }

        .car-container {
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 5px;
            margin: 20px;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        footer {
            background-color: #343a40;
            color: #fff;
            padding: 10px;
            text-align: center;
            position: fixed;
            bottom: 0;
            width: 100%;
        }

        .car-attribute {
            margin-bottom: 5px;
        }
    </style>
</head>

<body>
  <nav class="navbar navbar-dark bg-dark">
    
      <div class="container">
          <a class="navbar-brand" href="#"><%= title %></a>
          <% if (isUserLoggedIn) { %>
            <a class="btn btn-primary" href="/" style="margin-right: 10px;">Home</a> 
            <a class="btn btn-primary" href="/logout" style="margin-right: 10px;">Logout</a>
          <% } else { %>
            <a class="btn btn-primary" href="/login" style="margin-right: 10px;">Login</a>
          <% } %>
      </div>
  </nav>

  <div class="container">
    <div class="row">
        <% appointmentCars.forEach(function(car) { %>
        <div class="col-md-6">
            <div class="car-container">
                <h5 style="margin-bottom: 20px;"><%= car.date %></h5>
                <h5 style="margin-bottom: 18px;"><%= car.make %> <%= car.model %></h5>
                <p class="car-attribute"><strong>Car ID:</strong> <%= car.car_id %></p>
                <p class="car-attribute"><strong>Year:</strong> <%= car.year %></p>
                <p class="car-attribute"><strong>Price:</strong> $<%= car.price %></p>
                <p class="car-attribute"><strong>Mileage:</strong> <%= car.mileage %> miles</p>
                <p class="car-attribute"><strong>Report URL:</strong> <a href="<%= car.reportUrl %>">View Report</a></p>
                <p class="car-attribute"><strong>Location:</strong> <%= car.location %></p>
                <p class="car-attribute"><strong>Meet with:</strong> <%= car.sellerUsername %></p>

              </div>
        </div>
        <% }); %>
    </div>
</div>

  <footer style="margin-top: 20px;">
      <div class="container">Made by Zhengyan Hu with 💪🏼</div>
  </footer>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
