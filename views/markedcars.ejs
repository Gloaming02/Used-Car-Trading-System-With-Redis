<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            margin-bottom: 60px;
        }

        .car-container {
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 5px;
            margin: 20px;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        footer {
            background-color: #343a40;
            color: #fff;
            padding: 10px;
            text-align: center;
            position: fixed;
            bottom: 0;
            width: 100%;
        }

        .car-attribute {
            margin-bottom: 5px;
        }
    </style>
</head>

<body>
  <nav class="navbar navbar-dark bg-dark">
    
      <div class="container">
          <a class="navbar-brand" href="#"><%= title %></a>
          <% if (isUserLoggedIn) { %>
            <a class="btn btn-primary" href="/markedcars" style="margin-right: 10px;">View Marked Cars</a> 
            <a class="btn btn-primary" href="/logout" style="margin-right: 10px;">Logout</a>
          <% } else { %>
            <a class="btn btn-primary" href="/login" style="margin-right: 10px;">Login</a>
          <% } %>
      </div>
  </nav>

  <div class="container">
      <h1>Marked Cars</h1>
      <% markedCars.forEach(function(car) { %>
          <div class="car-container">
              <h5 style="margin-bottom: 20px;"><%= car.make %> <%= car.model %></h5>
              <p class="car-attribute"><strong>Car ID:</strong> <%= car.car_id %></p>
              <!-- Add other car attributes here -->
              <form action="/unmarkcar" method="POST">
                  <input type="hidden" name="car_id" value="<%= car.car_id %>">
                  <button type="submit" class="btn btn-danger">Remove Mark</button>
              </form>
          </div>
      <% }); %>
  </div>

  <footer style="margin-top: 20px;">
      <div class="container">Made by Zhengyan Hu with 💪🏼</div>
  </footer>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
